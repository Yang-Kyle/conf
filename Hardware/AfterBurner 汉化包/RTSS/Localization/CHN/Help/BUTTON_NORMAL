应用检测级别：正常
使用正常检测级别检测与当前配置文件关联的应用程序。在这种模式下， %PRODUCTNAME% 仅截听应用程序对Direct3D和OpenGL运行时库的调用。
在大多数现代3D程序中收集的性能相关的统计数据（比如帧率）并使用其他附加服务（比如OSD）时，建议使用该模式。

提示：
- 一些受保护的应用程序可能会将应用程序检测和统计信息收集视为可能的威胁，并可能由于这个原因拒绝运行。尝试降低此类应用程序的检测级别以排除此类问题。
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。