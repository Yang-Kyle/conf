应用程序检测级别：无
禁用与当前配置文件关联的应用程序的检测。在此模式下， %PRODUCTNAME% 不会收集运行时统计信息（例如帧率）也不会向关联的应用程序提供其他服务（例如OSD）。

提示：
- 一些受保护的应用程序可能会将用于程序检测，统计数据收集和OSD显示的可执行代码挂钩视为威胁而拒绝运行。要解决此类问题，请尝试添加此类应用程序的配置文件，并降低应用程序检测级别。
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。