启用与修改的Direct3D运行库的兼容性
默认情况下， %PRODUCTNAME% 仅支持使用使用官方Microsoft Direct3D运行库的Direct3D应用程序。您可以使用此选项使其与使用其他定制过的Direct3D运行库（例如，启用了InjectFXAA的Direct3D应用程序）的Direct3D应用程序兼容。
此选项是与配置文件绑定的，因此可以为不同的应用程序独立配置。不建议全局启用它，因为兼容性模式可能会导致某些Direct3D应用程序无法启动。

提示：
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。