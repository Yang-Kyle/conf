帧尺寸
当以满帧大小的高分辨率捕获视频时，PCIE总线带宽可能会成为限制您的帧速率的瓶颈，因为每个帧都要通过总线从GPU复制到CPU大量数据。 
在这种情况下，您可以尝试通过减小帧大小来提高性能，例如，选择1/2帧大小而不是全帧将减少要从GPU传输到CPU的数据量。选择1/4帧减少帧大小能通过更有效减少要压缩和写入HDD的数据量来减少CPU负载和HDD负载。   

提示：
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。