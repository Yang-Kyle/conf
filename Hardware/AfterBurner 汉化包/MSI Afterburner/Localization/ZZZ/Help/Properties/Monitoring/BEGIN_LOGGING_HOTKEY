开始记录热键
允许定义全局热键，开始将硬件监视历史记录记录到文件中。

提示：
- %PRODUCTNAME% 以原生RivaTuner日志文件格式保存历史记录。您可以使用内置浏览器或RivaTuner查看使用%PRODUCTNAME%记录的日志文件。
- 硬件监控历史记录在性能方面不是绝对无消耗的，每次日志更新需要一些CPU占用才能将数据写入硬盘。 
- 某些热键组合（例如<1> ... <5>，<Ctrl> + <1> ... <Ctrl> + <5>和<Ctrl> + <D>）被保留，不能使用。
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。