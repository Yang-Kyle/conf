主动OSD布局属性
显示当前主动的OSD布局的属性列表，并允许自定义。 

提示：
- 您可以通过双击或通过选择并按<Enter>键更改属性。
- 如果您按住<Ctrl>键双击，可以更改与某些属性相关联的附加设置。例如，您可以按住<Ctrl>键，同时双击<对齐>节点中的属性可在固定字体大小和可变字体大小对齐模式之间切换。
- 您可以通过选择根节点或其中一个子项并按下<Insert>键将子项添加到某些属性节点（例如，颜色库）。
- 您可以通过选择所需项目并按<Delete>键从某些属性节点（例如颜色库）中删除子项。
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。