配置文件插槽3热键
允许定义全局热键，用于从配置文件插槽3加载和应用配置文件。

提示：
- 在3D应用程序运行期间，应用超频配置文件时要小心。根据显卡型号和显示驱动程序架构，可能会导致副作用并降低系统的稳定性。
- 某些热键组合（例如<1> ... <5>，<Ctrl> + <1> ... <Ctrl> + <5>和<Ctrl> + <D>）被保留，不能使用。
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。